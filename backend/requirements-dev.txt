# Development/CI-only dependencies. Never installed in a production image.
# Keeping these separate from requirements.txt means a production install
# never pulls test tooling it doesn't need.
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-timeout==2.3.1
ruff==0.7.4
pip-audit==2.7.3
